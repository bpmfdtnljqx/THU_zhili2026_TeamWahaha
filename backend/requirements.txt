fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.5   # required for UploadFile / multipart form data

# ── Recommendation engine dependencies ──
torch
sentence-transformers
chromadb
python-dotenv
requests

# ── Recognition engine dependencies ──
httpx>=0.27.0   # async-capable HTTP client for Auris provider
